# Architecture Diagram: Before & After Refactoring

## BEFORE: Monolithic Architecture (975 lines)

```
┌─────────────────────────────────────────────────────────┐
│                 AthleteUnknown.tsx                      │
│                     (975 lines)                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌──────────────────────────────────────────────┐     │
│  │ Helper Functions (inline)                    │     │
│  │  - lev() - Levenshtein distance             │     │
│  │  - normalize() - String normalization        │     │
│  │  - camelCaseToTitleCase()                   │     │
│  │  - evaluateRank()                           │     │
│  │  - formatDate()                             │     │
│  │  - getGuestSessionKey()                     │     │
│  │  - saveGuestSession()                       │     │
│  │  - loadGuestSession()                       │     │
│  │  - clearGuestSession()                      │     │
│  │  - clearAllGuestSessions()                  │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
│  ┌──────────────────────────────────────────────┐     │
│  │ State (30+ variables)                       │     │
│  │  - activeSport, isRulesModalOpen,           │     │
│  │  - gameState[sport] containing:             │     │
│  │    * playerData, roundStats, score,         │     │
│  │    * flippedTiles, photoRevealed,           │     │
│  │    * message, hint, finalRank,              │     │
│  │    * tilesFlippedCount, incorrectGuesses,   │     │
│  │    * isLoading, error, gaveUp, etc.         │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
│  ┌──────────────────────────────────────────────┐     │
│  │ Business Logic                               │     │
│  │  - handleNameSubmit() - 85 lines           │     │
│  │  - handleTileClick() - 95 lines            │     │
│  │  - handleGiveUp()                          │     │
│  │  - handleShare()                           │     │
│  │  - updateState()                           │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
│  ┌──────────────────────────────────────────────┐     │
│  │ Side Effects (useEffect)                    │     │
│  │  - Save activeSport to localStorage         │     │
│  │  - Load player data from API                │     │
│  │  - Submit game results                      │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
│  ┌──────────────────────────────────────────────┐     │
│  │ UI Rendering (JSX)                          │     │
│  │  - 500+ lines of nested JSX                │     │
│  │  - Sports navbar, score display,            │     │
│  │  - Tile grid, modals, etc.                  │     │
│  └──────────────────────────────────────────────┘     │
│                                                         │
└─────────────────────────────────────────────────────────┘

PROBLEMS:
❌ Hard to find specific logic
❌ Can't test logic without mounting component
❌ Can't reuse logic in other components
❌ High cognitive load (too much in one place)
❌ Merge conflicts when multiple devs work on it
```

---

## AFTER: Modular Architecture (1,200 lines across 13 files)

```
┌──────────────────────────────────────────────────────────────────────┐
│                      AthleteUnknown.tsx                              │
│                        (436 lines)                                   │
│                   ⬇️ ONLY UI & COMPOSITION ⬇️                         │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  Local State: activeSport, isRulesModalOpen, etc.                  │
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐      │
│  │ useGameState   │  │ useGameData    │  │ useGuestSession│      │
│  │                │  │                │  │                │      │
│  │ Returns:       │  │ Auto-loads:    │  │ Auto-saves:    │      │
│  │ - state        │  │ - playerData   │  │ - game state   │      │
│  │ - updateState  │  │ - roundStats   │  │                │      │
│  └────────────────┘  └────────────────┘  └────────────────┘      │
│                                                                      │
│  ┌────────────────┐  ┌────────────────┐  ┌────────────────┐      │
│  │ useGameLogic   │  │ useTileFlip    │  │ useShareResults│      │
│  │                │  │                │  │                │      │
│  │ Returns:       │  │ Returns:       │  │ Returns:       │      │
│  │ - handleSubmit │  │ - handleClick  │  │ - handleShare  │      │
│  │ - handleGiveUp │  │                │  │                │      │
│  └────────────────┘  └────────────────┘  └────────────────┘      │
│                                                                      │
│  UI Rendering: Sports nav, tiles, modals, score display           │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              │ Uses
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│                     CUSTOM HOOKS LAYER                               │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  src/features/game/hooks/                                          │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ useGameState.ts (89 lines)                                  │  │
│  │ ┌─────────────────────────────────────────────────────────┐ │  │
│  │ │ - Manages game state for all 3 sports                  │ │  │
│  │ │ - updateState(patch) - merge partial state             │ │  │
│  │ │ - resetState() - clear state                           │ │  │
│  │ │ - Preserves currentPlayerIndex automatically           │ │  │
│  │ └─────────────────────────────────────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ useGameLogic.ts (119 lines)                                 │  │
│  │ ┌─────────────────────────────────────────────────────────┐ │  │
│  │ │ - handleNameSubmit() - validates guesses               │ │  │
│  │ │   * Checks Levenshtein distance                        │ │  │
│  │ │   * Handles "close" guesses (≤3 edit distance)         │ │  │
│  │ │   * Prevents duplicate consecutive guesses             │ │  │
│  │ │   * Updates score, rank, hints                         │ │  │
│  │ │ - handleGiveUp() - sets gave up state                  │ │  │
│  │ └─────────────────────────────────────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ useTileFlip.ts (107 lines)                                  │  │
│  │ ┌─────────────────────────────────────────────────────────┐ │  │
│  │ │ - handleTileClick(index) - flips tiles                 │ │  │
│  │ │   * Regular tiles: deduct 3 points                     │ │  │
│  │ │   * Photo tile: deduct 6 points, reveal photo puzzle   │ │  │
│  │ │   * Tracks first/last tile flipped                     │ │  │
│  │ │   * Handles photo reveal animations                    │ │  │
│  │ │   * No score change if game already won/gave up        │ │  │
│  │ └─────────────────────────────────────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ useGameData.ts (134 lines)                                  │  │
│  │ ┌─────────────────────────────────────────────────────────┐ │  │
│  │ │ - useEffect: Loads player data & round stats on mount │ │  │
│  │ │   * Fetches in parallel for performance               │ │  │
│  │ │   * Handles loading/error states                      │ │  │
│  │ │ - useEffect: Submits results when player wins         │ │  │
│  │ │   * Checks localStorage to prevent duplicate submits  │ │  │
│  │ │   * Updates round stats after submission              │ │  │
│  │ │ - refetchData() - manual data reload                  │ │  │
│  │ └─────────────────────────────────────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ useGuestSession.ts (46 lines)                               │  │
│  │ ┌─────────────────────────────────────────────────────────┐ │  │
│  │ │ - useEffect: Auto-saves game state to localStorage    │ │  │
│  │ │ - useEffect: Loads saved session on mount             │ │  │
│  │ │   * Only loads if same player/puzzle                  │ │  │
│  │ │   * Clears old sessions for different puzzles         │ │  │
│  │ │ - clearSession() - manual clear                       │ │  │
│  │ └─────────────────────────────────────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
│  ┌─────────────────────────────────────────────────────────────┐  │
│  │ useShareResults.ts (44 lines)                               │  │
│  │ ┌─────────────────────────────────────────────────────────┐ │  │
│  │ │ - handleShare() - copies results to clipboard         │ │  │
│  │ │   * Generates emoji grid (🟨 flipped, 🟦 unflipped)  │ │  │
│  │ │   * Includes puzzle # and score                       │ │  │
│  │ │   * Shows "copied" message for 3 seconds              │ │  │
│  │ └─────────────────────────────────────────────────────────┘ │  │
│  └─────────────────────────────────────────────────────────────┘  │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘
                              │
                              │ Uses
                              ▼
┌──────────────────────────────────────────────────────────────────────┐
│                      UTILITIES LAYER                                 │
├──────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  src/features/game/utils/                                          │
│                                                                      │
│  ┌───────────────────────────────────────┐                         │
│  │ stringMatching.ts (35 lines)         │                         │
│  │ • calculateLevenshteinDistance()     │  ← Used by useGameLogic │
│  │ • normalize()                        │                         │
│  └───────────────────────────────────────┘                         │
│                                                                      │
│  ┌───────────────────────────────────────┐                         │
│  │ scoring.ts (59 lines)                │                         │
│  │ • calculateNewScore()                │  ← Used by hooks        │
│  │ • evaluateRank()                     │                         │
│  │ • generateHint()                     │                         │
│  └───────────────────────────────────────┘                         │
│                                                                      │
│  ┌───────────────────────────────────────┐                         │
│  │ guestSession.ts (90 lines)           │                         │
│  │ • saveGuestSession()                 │  ← Used by useGuest     │
│  │ • loadGuestSession()                 │     Session             │
│  │ • clearGuestSession()                │                         │
│  │ • clearAllGuestSessions()            │                         │
│  └───────────────────────────────────────┘                         │
│                                                                      │
│  src/utils/                                                         │
│                                                                      │
│  ┌───────────────────────────────────────┐                         │
│  │ formatting.ts (29 lines)             │                         │
│  │ • camelCaseToTitleCase()             │  ← Used by component    │
│  │ • formatDate()                       │                         │
│  └───────────────────────────────────────┘                         │
│                                                                      │
│  src/constants/                                                     │
│                                                                      │
│  ┌───────────────────────────────────────┐                         │
│  │ storage.ts (23 lines)                │                         │
│  │ • STORAGE_KEYS constants             │  ← Used everywhere      │
│  │ • getGuestSessionKey()               │                         │
│  │ • getGameSubmissionKey()             │                         │
│  └───────────────────────────────────────┘                         │
│                                                                      │
└──────────────────────────────────────────────────────────────────────┘

BENEFITS:
✅ Easy to find specific logic (clear file names)
✅ Can test each hook/utility independently
✅ Can reuse logic across components
✅ Low cognitive load (small, focused files)
✅ Fewer merge conflicts (different devs work on different files)
✅ Each module has single responsibility
```

---

## Data Flow Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER ACTIONS                            │
└─────────────────────────────────────────────────────────────────┘
            │                    │                   │
            │                    │                   │
        Click Tile          Submit Name         Click Share
            │                    │                   │
            ▼                    ▼                   ▼
   ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
   │  useTileFlip    │  │  useGameLogic   │  │ useShareResults │
   │                 │  │                 │  │                 │
   │ handleTileClick │  │ handleNameSubmit│  │  handleShare    │
   └─────────────────┘  └─────────────────┘  └─────────────────┘
            │                    │                   │
            └────────────────────┴───────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │  updateState(patch)    │ ← From useGameState
                    └────────────────────────┘
                                 │
                                 ▼
                    ┌────────────────────────┐
                    │   Merged State         │
                    │   (immutable update)   │
                    └────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
                    ▼                         ▼
       ┌────────────────────────┐  ┌─────────────────────┐
       │  useGuestSession       │  │  Component          │
       │  Auto-saves to         │  │  Re-renders with    │
       │  localStorage          │  │  new state          │
       └────────────────────────┘  └─────────────────────┘
```

---

## Hook Dependencies Map

```
AthleteUnknown.tsx
    │
    ├─► useGameState ──────────────────┐
    │       │                          │
    │       └─► Returns: { state, updateState }
    │                                  │
    ├─► useGameData ◄──────────────────┤
    │       Uses: updateState          │
    │       Fetches: API data          │
    │                                  │
    ├─► useGuestSession ◄──────────────┤
    │       Uses: updateState          │
    │       Calls: guestSession.ts     │
    │                                  │
    ├─► useGameLogic ◄─────────────────┤
    │       Uses: updateState          │
    │       Calls: stringMatching.ts   │
    │       Calls: scoring.ts          │
    │                                  │
    ├─► useTileFlip ◄──────────────────┤
    │       Uses: updateState          │
    │       Calls: scoring.ts          │
    │                                  │
    └─► useShareResults ◄──────────────┘
            Uses: updateState
```

---

## Testing Strategy

```
┌────────────────────────────────────────────────────────────┐
│                    TESTING PYRAMID                         │
└────────────────────────────────────────────────────────────┘

                        ▲
                       ╱│╲
                      ╱ │ ╲
                     ╱  │  ╲
                    ╱   │   ╲
                   ╱    │    ╲
                  ╱ E2E │     ╲        ← Test full game flow
                 ╱──────┼──────╲         (Cypress/Playwright)
                ╱       │       ╲
               ╱  INTEGRATION   ╲     ← Test hooks with
              ╱─────────┼─────────╲      React Testing Library
             ╱          │          ╲
            ╱           │           ╲
           ╱     UNIT TESTS         ╲  ← Test utilities & pure
          ╱──────────────────────────╲    functions (Jest)
         ╱________________________________╲

EASY TO TEST NOW:

Unit Tests (Pure Functions):
  ✅ stringMatching.ts - No React needed
  ✅ scoring.ts - No React needed
  ✅ formatting.ts - No React needed
  ✅ guestSession.ts - Mock localStorage

Integration Tests (Hooks):
  ✅ useGameState - @testing-library/react-hooks
  ✅ useGameLogic - Mock dependencies
  ✅ useTileFlip - Test state updates
  ✅ useGameData - Mock API calls
  ✅ useGuestSession - Mock storage
  ✅ useShareResults - Mock clipboard API

Component Tests:
  ✅ AthleteUnknown - Mock all hooks
  ✅ Test UI interactions
  ✅ Test modal open/close
  ✅ Test sport switching
```

---

## Performance Optimizations

```
BEFORE: No memoization
  ❌ Functions recreated on every render
  ❌ Child components re-render unnecessarily

AFTER: Optimized with useCallback
  ✅ All event handlers wrapped in useCallback
  ✅ Dependencies carefully managed
  ✅ Prevents unnecessary re-renders

Example:
  const handleTileClick = useCallback((index: number) => {
    // ... logic ...
  }, [state, updateState]);  // Only recreate if deps change
```

---

## Scalability

```
ADDING NEW FEATURES:

Before (Monolithic):
  ❌ Edit 975-line file
  ❌ Risk breaking existing features
  ❌ Hard to review changes
  ❌ Merge conflicts likely

After (Modular):
  ✅ Create new hook file
  ✅ Compose in component
  ✅ Easy to review (small file)
  ✅ No conflicts with other features

Example - Adding "Undo" feature:
  1. Create useUndoRedo.ts (50 lines)
  2. Add to component: const { undo, redo } = useUndoRedo(state)
  3. Add UI buttons
  4. Done! No changes to existing hooks
```

---

## Summary

This refactoring transforms unmaintainable spaghetti code into a clean,
professional architecture following React best practices and SOLID principles.

**Key Achievement:** Reduced main component complexity by 55% while improving
testability, maintainability, and developer experience.
